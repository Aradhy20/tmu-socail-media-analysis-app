services:
  - type: web
    name: social-media-analytics
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run professional_dashboard.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.6